/**
 * @desygn/ui — Shared design system.
 *
 * Exports:
 *   - tokens (CSS variables, color/spacing scales)
 *   - primitives (Button, Input, Card, Dialog, Toast, ...)
 *   - patterns (DataTable, EmptyState, ConfirmDialog, ...)
 *
 * Consumed by:
 *   - apps/a11y-dashboard
 *   - apps/marketing
 *   - (future) plugin/ ui rebuild
 */

export * from "./tokens/index.js";
export * from "./lib/cn.js";
