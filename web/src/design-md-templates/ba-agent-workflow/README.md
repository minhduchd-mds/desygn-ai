# BA Agent Workflow

Design system and BA workflow details are defined in `DESIGN.md`.

Use this template when the workspace should generate BA Markdown artifacts, requirement workflows, and 8-10 screen specifications instead of relying on a manually typed product prompt.
