name: Source-Policy

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  provenance-and-repository-policy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Require source provenance policy
        shell: bash
        run: |
          set -euo pipefail
          test -s docs/SOURCE_PROVENANCE.md

      - name: Reject unresolved merge markers
        shell: bash
        run: |
          set -euo pipefail
          if git grep -n -E '^(<<<<<<< |=======|>>>>>>> )' -- \
            ':!package-lock.json' ':!**/package-lock.json' ':!*.min.js'; then
            echo 'Unresolved Git conflict markers found.' >&2
            exit 1
          fi

      - name: Reject committed environment secrets
        shell: bash
        run: |
          set -euo pipefail
          tracked_env="$(git ls-files | grep -E '(^|/)\.env($|\.)' | grep -v -E '\.env\.example$' || true)"
          if [ -n "$tracked_env" ]; then
            echo 'Tracked environment files are not allowed:' >&2
            echo "$tracked_env" >&2
            exit 1
          fi

      - name: Policy summary
        run: |
          echo 'Source provenance present.'
          echo 'No unresolved conflict markers found.'
          echo 'No tracked environment secret files found.'
